version: '3.8'

services:
  scanner:
    build: .
    container_name: datarover-scanner
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - datarover-network

networks:
  datarover-network:
    driver: bridge
